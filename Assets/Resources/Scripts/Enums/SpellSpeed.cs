public enum SpellSpeed
{
    None,
    Slow,       // Opponent can respond before resolution
    Fast,       // Opponent can respond after resolution
    Instant,    // Opponent cannot respond
    Final,      // End turn after resolution
}
